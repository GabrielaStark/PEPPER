# Configuración externa de Grails, generada por PEPPER (rehydrate.extra_templates).
# Config.groovy:22-38 la lee de ~/.grails/<app>-config.properties y la fusiona sobre la del WAR.
# Solo se sobreescribe la URL del datasource: la del WAR trae parámetros que el motor de la
# versión del respaldo rechaza (ver la desviación en environment.json). Usuario y contraseña
# siguen siendo los compilados en DataSource.groovy.
dataSource.url={{db_url}}
